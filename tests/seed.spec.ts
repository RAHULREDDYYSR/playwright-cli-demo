// spec: specs/demoblaze.plan.md
// Seed test for demoblaze - lands the page in fresh state for every scenario
import { test } from './fixtures';

test('seed', async ({ page }) => {
  // Fixture already navigates to baseURL. This empty body tells agents where to start.
});
